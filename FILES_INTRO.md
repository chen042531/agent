# 專案檔案介紹 (Files Intro)

本專案將 AI Agent 的邏輯拆分得非常乾淨，主要分為四個核心檔案。
以下為每個檔案的功能與實作原理介紹：

## 1. `main.py` (主程式進入點與終端機介面)
**職責：** 負責啟動程式、與使用者互動、以及處理終端機的特殊指令。
**介紹：**
- 它是您執行 `python main.py` 時第一個跑起來的檔案。
- 裡面包含一個無窮迴圈（`while True:`），不斷等待您在終端機輸入訊息。
- 具備**指令解析器**：如果您的輸入以 `/` 開頭（例如 `/tools` 或 `/toggle`），它會攔截這些指令並直接呼叫 Agent 的管理功能，而不會把這些指令當作聊天訊息傳給 AI。
- 若不是特殊指令，它就會將您的文字交給 `agent.py` 去思考，並將結果印回終端機。

## 2. `agent.py` (核心大腦邏輯)
**職責：** 管理對話紀錄、處理與 Ollama API 的連線、以及調度工具。
**介紹：**
- **對話記憶 (Memory)**：裡面有一個 `self.messages` 陣列，負責記住所有的對話上下文，包含一開始設定的 System Prompt。
- **權限管家**：內建 `self.permissions` 字典，負責記錄每一個工具目前是「開啟」還是「關閉」的狀態。
- **Tool Calling (工具呼叫) 迴圈**：`chat()` 函式是這支程式的靈魂。當把使用者的問題傳給 Gemma 4 後，模型如果回傳了 `tool_calls`（表示模型想要用工具），`agent.py` 會攔截這個請求：
  1. 先去檢查權限，若沒權限就退回一句「Permission denied」。
  2. 若有權限，就去呼叫 `tools.py` 中對應的函式。
  3. 將函式執行的結果（例如搜尋到的網頁文字）包裝成 `role: tool` 的訊息再丟回給模型。
  4. 重複這個過程，直到模型看懂工具回傳的結果並給出最終答案為止。

## 3. `tools.py` (代理人工具箱)
**職責：** 實作各式各樣的 Python 函式，這些就是 Agent 的「手腳」。
**介紹：**
- 這裡面的每一個函式（例如 `web_search`, `read_file` 等），都必須寫好清楚的 **Docstring (註解)** 和**型別提示 (Type Hint)**。
- 為什麼註解很重要？因為 Ollama 的 Python 套件會自動讀取這些 Python 函式的註解與參數說明，並轉換成 JSON Schema 傳給 Gemma 4。模型就是靠讀這些註解，才知道什麼時候該用什麼工具！
- **可擴充性**：如果您未來想讓 Agent 能查股票、寄 Email 等等，只要在這個檔案裡新增函式，然後加進檔案最下方的 `AVAILABLE_TOOLS` 陣列中即可。

## 4. `requirements.txt` (套件依賴清單)
**職責：** 告訴 Python 環境這個專案需要安裝哪些第三方套件。
**介紹：**
- `ollama`：Ollama 的官方 Python SDK，用來跟本地端的 Ollama 伺服器溝通，原生支援傳遞 Python 函式作為 tools。
- `duckduckgo-search`：一個輕量級的網路搜尋套件，讓 Agent 能抓取最新的網路資訊，不需要申請任何複雜的 API Key。
